import 'dart:ui';

import 'package:flutter/material.dart';

/// 화면 디자인에 이용한 테마 컬러들을 명시한 클래스이다.
class ThemeColor {
  static Color main = Color(0xFFBACEE0);
  static Color background = Colors.white;
  static Color text = Color(0xFF191919);
}